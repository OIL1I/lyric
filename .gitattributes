# Line endings are part of the test contract here, not a formatting preference: the golden
# snapshots of the lexer and the parser compare span offsets, and CRLF shifts every offset by one
# byte per line.
#
# `eol=lf` forces LF in the working tree as well, not only in the object store.
* text=auto eol=lf
