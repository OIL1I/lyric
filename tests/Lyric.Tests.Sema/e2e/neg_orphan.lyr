// Negative: an orphan extension (LYR-SEM0041). Neither the target (the builtin string) nor the
// interface (imported from a foreign module) is declared locally here.

import std.core { Comparable };

extend string :: [Comparable] {
    fn compareTo(other: string): int {
        return 0;
    }
}

fn main(): int {
    return 0;
}
