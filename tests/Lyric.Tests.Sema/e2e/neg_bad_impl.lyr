// Negative: interface conformance with a wrong signature (the return type) gives LYR-SEM0042.

interface Priced {
    fn price(): int;
}

struct Item :: [Priced] {
    name: string,
    fn price(): string {          // should be int
        return this.name;
    }
}

fn main(): int {
    return 0;
}
