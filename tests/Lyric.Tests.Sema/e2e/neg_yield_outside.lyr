// Negative: a yield outside a coroutine, where the return type is no Coroutine<T>
// → LYR-SEM0038.

fn notACoroutine(): int {
    yield 1;
    return 0;
}

fn main(): int {
    return notACoroutine();
}
