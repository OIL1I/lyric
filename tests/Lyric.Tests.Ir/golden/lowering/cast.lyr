fn widen(x: int32): int64 {
    return x as int64;
}

// Identitaets-Cast: das Lowering elidiert ihn, es entsteht kein 'convert'.
fn same(x: int): int {
    return x as int;
}

fn toFloat(x: int): float {
    return x as float;
}
