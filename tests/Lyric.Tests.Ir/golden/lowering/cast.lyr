fn widen(x: int32): int64 {
    return x as int64;
}

// An identity cast: the lowering elides it and no 'convert' arises.
fn same(x: int): int {
    return x as int;
}

fn toFloat(x: int): float {
    return x as float;
}
