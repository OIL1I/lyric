# The plugin's own version, aligned with the extension's rather than with the toolchain's:
# both are clients of the same server and gain features on their own cadence.
pluginVersion=1.2.0

org.gradle.caching=true
org.gradle.configuration-cache=true
kotlin.stdlib.default.dependency=false
